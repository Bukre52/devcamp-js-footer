## Devcamp JS Footer

> This should be used in the following manner

Install with command:

```
npm install --save  firtnpm
```

Add to a JavaSfcript Project with the following code:

```javascript
import ( footer ) from 'firstnpm';

footer('some name')
```